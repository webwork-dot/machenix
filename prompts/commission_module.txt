[function_name] I want to change the sales_commission saving logic while approving sale order.
Currently I am saving commission amount by product total sale amount and then calculate product profit percentage from product commission.
But now I wanna store commission batch wise.. and the price calculation will also get changed.

This is the logic you must follow in order to change the commission module:

1). Inside batch first take the [actual_cost_with_exp] price and product amount in batch [sale_batch_amt]. Then check if the sale product price is less than batch price then don't give commission otherwise check the difference in percentage like this (([sale_batch_amt] / [actual_cost_with_exp]) * 100). That way you will get profit percentage.

2). Use product_id to get [commission_id] and then use this id to get [product_commission_slab] row data. Use the percentage from [product_commission_slab] to calculate commission amount from product sale amount [sale_batch_amt] using this formula (([sale_batch_amt] * [sale_batch_qty] / (1 + ([commission_percentage] / 100)))) and save it here [sales_commission table]

3). If product doesn't have commission_id then skip commission saving process. If it has commission_id then use the profit percentage to get the profit range percentage and profit_id. Use this condition ([profit_commission_slab - comm_from] >= profit percentage) and ([profit_commission_slab - comm_to] <= profit percentage) to get the profit commission and use the profit id to get staff assigned profit from here [staff_commission] using this condition ([sales_order - sale_person_id] = [staff_commission - staff_id])([profit_commission_slab - id] = [staff_commission - profit_id]), If no profit found or staff commission found then skip the commission saving process. If it found then save the profit_commission data here [sales_commission] and staff commission customer and distributor percentage here [sales_commission], After this check the customer if he is distributor or not from here [sales_order - is_distributor]. If he is dributor then use distributor percentage other wise customer percentage to calculate commission amount.. Here is the formula to calculate commission amount. (([sale_batch_amt] * [sale_batch_qty]) * ([sales_commission] / (100 + [sales_commission]))). Take commission percentage based on customer type.. If the commission amount is zero then skip the commission saving process.

4). If the customer has shared staff then save the shared staff id here [sales_commission - share_staff_id].. Check if the profit id of sales exist here or not.. If the entry hasn't existed then pass zero here [sales_commission] and 100 here [sales_commission]

- I want you to follow the existing pattern of coding. Also create a seperate function for handling commission logic.. Pass the sale order id and then inside function run a batch loop to handle the commission logic. Call this helper function inside both functions to avoid repeatability... Follow existing coding patterns